name: authentication
description: A authentication package.
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.9.2
  flutter: '>=3.0.0'

dependencies:
  core: ^0.0.1
  dependencies: ^0.0.1
  flutter:
    sdk: flutter
  mask_text_input_formatter: ^2.9.0

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
