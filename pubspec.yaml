name: did_app_poc
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.9.2

dev_dependencies:
  flutter_lints: ^5.0.0

workspace:
  - app
  - common/dependencies
  - common/core
  - common/network
  - common/shell
  - features/authentication
  - features/user_credentials
dependencies:
  glassmorphism: ^3.0.0
  stacked_card_carousel: ^0.0.4
